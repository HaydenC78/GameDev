public class Enemy extends GameObject {
    public Enemy(int x, int y, int width, int height, String imagePath) {
        // Use the actual variables passed into the constructor
        super(x, y, width, height, imagePath);
    }
}